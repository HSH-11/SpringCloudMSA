server:
  port: 8888

spring:
  application:
    name: config-service
  profiles:
    active: git
  cloud:
    config:
      server:
        git: #default
          uri: https://github.com/HSH-11/spring-cloud-config
          default-label: master
